# Generated by Misoto Harness; DO NOT EDIT. version=0.2.15 source=b1b52c985c67 profiles=web-typescript content=38861cc061c3b3ac
name: Misoto Harness

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          # The rulesync toolchain installs through pnpm below; leave the consumer
          # repository's own package manager cache alone.
          package-manager-cache: false
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          package_json_file: .rulesync/package.json
      - run: python .rulesync/harness-check.py
        env:
          MISOTO_HARNESS_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
      - run: pnpm --dir .rulesync install --ignore-workspace --frozen-lockfile
      - run: ./.rulesync/node_modules/.bin/rulesync generate --check --silent
